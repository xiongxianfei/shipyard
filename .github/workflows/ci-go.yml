name: CI — Go

on:
  push:
    branches: [main]
    paths:
      - 'go/**'
      - '.github/workflows/ci-go.yml'
  pull_request:
    branches: [main]
    paths:
      - 'go/**'
      - '.github/workflows/ci-go.yml'
  schedule:
    - cron: '0 3 * * 1'  # every Monday 03:00 UTC
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build and smoke-test Go image
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build image (PR / schedule — load locally for smoke test)
        if: github.event_name != 'push' || github.ref != 'refs/heads/main'
        uses: docker/build-push-action@v6
        with:
          context: ./go
          push: false
          load: true
          tags: dev-go:ci
          cache-from: type=gha,scope=go
          cache-to:   type=gha,scope=go,mode=max

      - name: Smoke test
        if: github.event_name != 'push' || github.ref != 'refs/heads/main'
        run: |
          docker run --rm dev-go:ci go version
          docker run --rm dev-go:ci air -v
          docker run --rm dev-go:ci dlv version
          docker run --rm dev-go:ci golangci-lint --version
          docker run --rm dev-go:ci staticcheck -version
          docker run --rm dev-go:ci gopls version

      - name: Build and push to GHCR (main branch only)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/build-push-action@v6
        with:
          context: ./go
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/dev-go:latest
            ghcr.io/${{ github.repository_owner }}/dev-go:sha-${{ github.sha }}
          cache-from: type=gha,scope=go
          cache-to:   type=gha,scope=go,mode=max
