name: CI — Java

on:
  push:
    branches: [main]
    paths:
      - 'java/**'
      - '.github/workflows/ci-java.yml'
  pull_request:
    branches: [main]
    paths:
      - 'java/**'
      - '.github/workflows/ci-java.yml'
  schedule:
    - cron: '0 3 * * 1'  # every Monday 03:00 UTC
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build and smoke-test Java image
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build image (PR / schedule — load locally for smoke test)
        if: github.event_name != 'push' || github.ref != 'refs/heads/main'
        uses: docker/build-push-action@v6
        with:
          context: ./java
          push: false
          load: true
          tags: dev-java:ci
          cache-from: type=gha,scope=java
          cache-to:   type=gha,scope=java,mode=max

      - name: Smoke test
        if: github.event_name != 'push' || github.ref != 'refs/heads/main'
        run: |
          docker run --rm dev-java:ci java -version
          docker run --rm dev-java:ci javac -version
          docker run --rm dev-java:ci mvn --version
          docker run --rm dev-java:ci gradle --version

      - name: Build and push to GHCR (main branch only)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/build-push-action@v6
        with:
          context: ./java
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/dev-java:latest
            ghcr.io/${{ github.repository_owner }}/dev-java:sha-${{ github.sha }}
          cache-from: type=gha,scope=java
          cache-to:   type=gha,scope=java,mode=max
